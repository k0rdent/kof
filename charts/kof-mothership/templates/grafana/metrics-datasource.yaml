{{- if .Values.grafana.enabled }}
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: {{ .Release.Name }}-metrics
  namespace: {{ .Release.Namespace }}
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  datasource:
    name: kof-metrics
    type: prometheus
    access: proxy
    {{- if .Values.kcm.kof.acl.enabled }}
    url: http://{{ .Release.Name }}-kof-acl:{{ .Values.kcm.kof.acl.port }}
    {{- else }}
    url: http://{{ .Release.Name }}-promxy:{{ .Values.promxy.service.servicePort }}
    {{- end }}
    isDefault: true
    jsonData:
      tlsSkipVerify: true
      timeInterval: "5s"
    {{- if .Values.kcm.kof.acl.enabled }}
      httpMethod: "GET"
      oauthPassThru: true
    {{- end }}
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: victoriametrics
  namespace: {{ .Release.Namespace }}
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  datasource:
    name: victoriametrics
    type: prometheus
    access: proxy
    url: http://vmselect-cluster:8481/select/0/prometheus
    isDefault: false
    jsonData:
      tlsSkipVerify: true
      timeInterval: "5s"
{{- end }}
