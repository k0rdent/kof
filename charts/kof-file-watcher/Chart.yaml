apiVersion: v2
name: kof-file-watcher
description: Deployment that watches a configurable filesystem path on a cluster node and emits logs and Prometheus metrics on changes or deletions.
type: application
version: "1.11.0-rc0"
appVersion: "1.11.0-rc0"
dependencies: []
