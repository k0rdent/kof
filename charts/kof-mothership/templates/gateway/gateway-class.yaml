{{- if and .Values.gateway.enabled .Values.gateway.createGatewayClass }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.gateway.spec.gatewayClassName }}
spec:
  controllerName: {{ .Values.gateway.gatewayClassControllerName }}
{{- end }}
